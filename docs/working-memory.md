# Working Memory

*Aktualizuj tento soubor po smysluplné práci. Záznamy nech konkrétní, krátké a užitečné pro další session.*

## Current goal

- Mít v repozitáři trvalou paměť agenta (AGENTS.md, .cursor/rules, docs/working-memory.md, docs/decisions.md) a po každé práci ji udržovat, aby bylo možné navázat v novém chatu.
- Připravit a následně spustit jednorázový importní skript, který z adresáře `data/` naplní archiv ročníků (fotky, program, základní výsledky) do Supabase Storage a DB, a doplnit na FE chytré fallback hlášky pro chybějící data.

## Current repository state

- **Stack:** Next.js 14, React 18, TypeScript, Supabase (DB + Auth), Tailwind CSS 3, lucide-react.
- **Veřejný web (1:1 dle ČŮČO.zip):** Hero – badge nadcházejícího ročníku (upcoming_event), dva CTA „Zobrazit poslední ročník“ / „Procházet archiv“. Navbar: O akci, Aktuální ročník, Archiv, Místo konání, Kontakt. O akci – bez testimoniálů; Soutěžní kategorie, Královna sklepa / Sračka roku, obrázky + 36+ ročníků. Aktuální ročník – tmavá sekce, vítězové (Královna, Bílá/Červená, Sračka, Cena diváků), karta Téma & program, Kompletní výsledky (accordion Bílá/Červená/Růžová), Fotogalerie + lightbox. Archiv – label „Archiv ročníků“, vyhledávání, přepínač Grid/Timeline, karty ročníků; klik otevře **modal** YearDetail (téma, vítězové, výsledky, galerie). Místo konání – tmavá sekce, text + 4 info karty, odkaz Otevřít mapu, foto. Kontakt – organizátoři (Petr Tyč, Libor Artur Martínek, Jiří Kača Zocher), Web a materiály (Honza Tyč, email). Footer – 3 sloupce (logo+text, Navigace, Kontakt), © 2025. Data: `fetchYearsWithData()` načte ročníky včetně výsledků a galerie ze Supabase. Stránka `/archiv/[id]` zůstává pro přímé odkazy. Barvy: DARK_WINE, WINE_RED, ACID_GREEN, LIGHT_BG.
- **Admin:** Přihlášení (Supabase Auth), dashboard, Nadcházející ročník (formulář + live preview), Archiv ročníků (CRUD). Na editaci ročníku (`/admin/years/[id]`): formulář ročníku + **program** (nadpis, autor, popis, obrázek/PDF z bucketu `year-program` nebo externí URL), **Výsledky soutěže** (manuální zadání), **Fotogalerie** (upload do Supabase Storage bucket `year-gallery`, řazení šipkami, mazání). Chráněné route pod `/admin` kromě `/admin/login`.
- **DB:** Všechny migrace nasazené: 20250312000000 (years, upcoming), 20250312100000 (upcoming_event, year_results_*, year_gallery, rozšíření years), 20250313000000 (Storage bucket `year-gallery` + RLS), 20250314020000 (tabulka `admin_users`), 20250314021000 (Storage bucket `year-program` + RLS), 20250314022000 (unikátní rok/název pro publikované ročníky).
- **Deploy:** GitHub repo, Vercel. Env na Vercelu: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY.

## Recent changes

- **Fix produkce: admin „Nadcházející ročník“ padal na `column user_id does not exist`:** Přidána migrace `supabase/migrations/20260427000100_fix_upcoming_event_policies.sql`, která na tabulce `public.upcoming_event` smaže všechny existující RLS policies a vytvoří konzistentní sadu (`upcoming_event_select`, `upcoming_event_all`) bez odkazu na neexistující sloupec `user_id`. Cíl: obnovit možnost čtení/uložení formuláře na `/admin/upcoming` i na DB se staršími policy variantami.
- **Galerie – klávesové ovládání lightboxu:** V `components/public/CurrentEdition.tsx` a `components/public/YearDetail.tsx` přidáno ovládání klávesami `ArrowLeft`/`ArrowRight` pro přepínání fotek a `Escape` pro zavření lightboxu.
- **Galerie – klikací přepínání šipkami vlevo/vpravo:** V lightboxu komponent `components/public/CurrentEdition.tsx` a `components/public/YearDetail.tsx` jsou šipky nově jako skutečná tlačítka s ikonami `ChevronLeft/ChevronRight` a přepínání je cyklické (z poslední fotky na první a opačně), takže lze galerie pohodlně procházet klikáním na šipky.
- **Favicon – burgundy podklad:** `app/icon.svg` byl upraven tak, že zelená sklenička (`#A7D129`) je nově zasazená na burgundy pozadí (`#3A0F16`) přes zaoblený podklad (`rect`).
- **Favicon ze zelené skleničky z loga:** Přidán soubor `app/icon.svg` se stejnou ikonou skleničky (lucide wine) v barvě `ACID_GREEN` (`#A7D129`) a v `app/layout.tsx` doplněno `metadata.icons` (`icon` + `shortcut` na `/icon.svg`), takže web používá zelenou skleničku jako favicon.
- **Archiv – rychlejší otevření detailu ročníku bez zpomalení initial loadu:** V `components/public/Archive.tsx` je nově klientská cache detailů ročníků (opakované otevření modalu je okamžité), prefetch detailu na hover/focus karty a nízkoprioritní idle prefetch jen pro několik prvních viditelných ročníků (se zpožděním). Cíl: zkrátit čekání po kliku na kartu v archivu, ale neprodloužit první načtení homepage.
- **Sekce „Co je Čůčobraní“ – nové lokální fotky (lossless WebP + cílený výřez):** V `components/public/About.tsx` byly nahrazeny externí Unsplash obrázky za nové lokální assety `public/about-cuco-rect.webp` a `public/about-cuco-square.webp` vytvořené z dodaných fotek. První obrázek je v obdélníku s fokusem na lidi vlevo (`objectPosition`), druhý je čtvercový výřez zaměřený na láhve a člověka. Oba soubory jsou uložené jako bezztrátové WebP.
- **Performance iterace 2 (defer detail archivu + menší payload):** `Archive` už neposílá/nenačítá těžká data pro všechny ročníky při prvním renderu. V `app/page.tsx` se do `Archive` předává odlehčený seznam (bez výsledkových tabulek a bez celé galerie) a detail ročníku se načítá až při kliknutí přes nové API `GET /api/years/[id]` (`app/api/years/[id]/route.ts`). V `components/public/Archive.tsx` je doplněn loading/error stav pro načítání detailu a `YearDetail` je lazy přes `next/dynamic`, takže modal bundle se stáhne až při otevření.
- **Nový server helper pro detail ročníku:** Do `lib/fetch-years.ts` přidána funkce `fetchYearWithDataById()` pro on-demand načtení jednoho ročníku (včetně výsledků, galerie a tématu), používá se v API route.
- **Performance iterace 1 (assety + caching + data fetch):** Veřejné stránky mají nově `revalidate = 300` (`app/page.tsx`, `app/archiv/[id]/page.tsx`) a používají `createPublicClient()` bez cookie vazby (`lib/supabase/public.ts`), aby šly lépe cachovat. `app/page.tsx` navíc lazy-loaduje těžší klientské sekce (`CurrentEdition`, `Archive`, `Location`, `Contact`) přes `next/dynamic`. `lib/fetch-years.ts` byl přepsán z N+1 modelu na bulk dotazy (`in("year_id", ids)` pro výsledky i galerii), čímž výrazně klesá počet roundtripů do DB/Storage.
- **Image optimalizace (public + komponenty):** Přidány moderní image formáty a remote patterns v `next.config.mjs` (`avif/webp`, Unsplash + Supabase). Hero používá `next/image` s jediným `priority` obrázkem (`components/public/Hero.tsx`). `About`, `Location`, `CurrentEdition` a `YearDetail` používají `next/image` pro hlavní/galerijní náhledy se `sizes` a lazy loadingem.
- **Asset cleanup na WebP:** Lokální obrázky v `public/` byly převedeny na WebP a přepojeny (`location-venue.webp`, `white-wine-glass.webp`, `404-background.webp`), původní velké PNG/JPG varianty byly odstraněny (`404-background.jpg/png`, `location-venue.png`, `white-wine-glass.png`, `404-photo.png`), čímž se zmenšila velikost statických assetů v repozitáři.
- **Hotová migrace staré galerie na WebP (Supabase Storage + DB):** Přidán jednorázový skript `scripts/migrate-gallery-to-webp.mjs` a npm příkaz `npm run migrate:gallery-webp` (vyžaduje `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`). Skript projde tabulku `year_gallery`, přeskočí externí URL a stávající `.webp`, ostatní obrázky stáhne z bucketu `year-gallery`, převede přes `sharp` na WebP, nahraje nový soubor, přepíše `storage_path` v DB a smaže původní soubor. V ostrém běhu převedeno 942 položek (941 + následný retry 1), průběžný dry-run po dokončení vrací `Rows to convert: 0`.
- **Galerie v adminu: nové uploady se vynuceně ukládají jako WebP:** V `app/admin/(protected)/years/YearGalleryForm.tsx` je při uploadu fotek přidán klientský převod JPEG/PNG -> WebP (canvas + `toBlob("image/webp")`). Do Storage bucketu `year-gallery` se nově ukládá vždy `.webp` s `contentType: "image/webp"`, takže budoucí nahrané obrázky mají konzistentně menší velikost. UI texty u uploadu/importu byly upravené, aby bylo jasné, že URL import se automaticky do WebP nepřevádí.
- **Velký úklid repozitáře (minimal footprint):** Smazány dočasné a pomocné artefakty, které nejsou nutné pro běh aplikace: adresáře `data/`, `figma-404/`, `figma-seed/`, `.next/`, všechny jednorázové skripty v `scripts/` (import/fix utility) a lokální pomocné soubory (`20200124_231809.jpg`, `cucobrani-web.code-workspace`, `dump_years_gallery.sql`, `tsconfig.tsbuildinfo`). V `package.json` byly odstraněny npm skripty `import:*` a `fix:*` a odinstalovány nepoužívané závislosti navázané na import (`adm-zip`, `sharp`, `dotenv`, `ts-node`, `@types/adm-zip`). Projekt nyní obsahuje jen runtime/admin části a nezbytné build nástroje.
- **Fix: mobilní layout už neprosakuje na desktop:** V `components/public/CurrentEdition.tsx` a `components/public/YearDetail.tsx` byl odstraněn inline `display: grid` z wrapperu mobilních karet (blok měl `md:hidden`, ale inline styl přepisoval Tailwind a karty se zobrazovaly i na desktopu). Wrapper teď používá `className="grid md:hidden"`, takže desktop znovu správně ukazuje pouze tabulku.
- **Mobilní karty výsledků – pořadí řádků upravené:** V `components/public/CurrentEdition.tsx`, `components/public/YearDetail.tsx` a `app/archiv/[id]/page.tsx` je v mobilních kartách nyní pořadí textů `Vzorek` → `Víno` (dříve opačně), podle aktuálního zadání.
- **Mobilní výsledky – další zpřesnění dle zadání:** V `components/public/CurrentEdition.tsx`, `components/public/YearDetail.tsx` a `app/archiv/[id]/page.tsx` byly zmenšené hodnoty pro pořadí a body na kartách a rozšířený fixní prostor pro pořadí (`w/width 3.35rem`, `nowrap`), aby se bez rozbití layoutu vešla i delší umístění jako `10.-11.`.
- **Mobilní výsledky – korekce 1:1 dle reference po feedbacku:** Ve `components/public/CurrentEdition.tsx`, `components/public/YearDetail.tsx` a `app/archiv/[id]/page.tsx` byla upravena mobilní typografie a spacing výsledkových karet (menší pořadí/body, menší paddings/gapy, kompaktnější text) aby odpovídaly referenčnímu screenshotu. Zároveň je přidána normalizace „Vzorek č.“ (`formatSampleLabel`), takže se už nezobrazuje chyba typu `Vzorek č.č.1`.
- **Mobilní výsledkové tabulky převedené na kartový layout dle reference:** V `components/public/CurrentEdition.tsx`, `components/public/YearDetail.tsx` a `app/archiv/[id]/page.tsx` se výsledky vín na mobilech (`< md`) nově renderují jako jednotlivé karty (pořadí vlevo, jméno + víno + „Vzorek č.“ uprostřed, body vpravo) vizuálně odpovídající screenshotu. Desktop (`md+`) zůstává tabulkový, aby se nezhoršila čitelnost na širokých displejích.
- **Místo konání – karta „Termín“ napojená na admin upcoming:** `components/public/Location.tsx` nově bere prop `upcomingDatetime` a v kartě „Termín“ zobrazuje stejnou hodnotu `datetime` z `upcoming_event`, která je vyplněná v adminu. `app/page.tsx` předává do `Location` stejný `upcomingDatetime`, který už používá Hero; fallback zůstává „Zimní období“.
- **Kontakty – aktualizace jmen/rolí/emailů + patička:** V `components/public/Contact.tsx` má Petr Tyč nově e-mail `tycak2@gmail.com`; Honza byl přejmenován na Jan Tyč s rolí „Média, fotografie, web“. V `components/public/Footer.tsx` je sekce kontaktů rozšířená na všechny tři osoby (Petr Tyč, Libor Artur Martínek, Jan Tyč) místo jednoho e-mailu.
- **Kontakt sekce – redesign dle reference:** `components/public/Contact.tsx` je přestylovaná podle screenshotu: nadpis vlevo + citát vpravo v hlavičce, pod tím 3 stejně velké kontaktní karty v řadě (Petr Tyč, Libor Artur Martínek, Honza Tyč) s kulatou ikonou, názvem, rolí a e-mailem odděleným horní linkou.
- **Sjednocení „Bílá vína“ ikony (highlight + modal):** `components/public/WhiteWineIcon.tsx` má novou variantu `size="label"` (1.2rem + jemné vertikální doladění). V `components/public/CurrentEdition.tsx` i `components/public/YearDetail.tsx` jsou accordion titulky „Bílá vína“ sjednocené přes stejný inline-flex wrapper + stejnou `WhiteWineIcon size="label"`, aby vizuálně odpovídaly emoji „Červená vína“ na obou místech.
- **Výsledky – shodné body sdílí pořadí:** Přidána utilita `lib/results-ranking.ts` (`formatPlaceWithTies`) a nasazena do veřejných výsledkových tabulek v `components/public/YearDetail.tsx`, `components/public/CurrentEdition.tsx` a `app/archiv/[id]/page.tsx`. Při shodě bodů se teď zobrazuje sdílené pořadí ve formátu např. `7.-8.` místo dvou odlišných pozic.
- **Modal detailu ročníku – CTA „Zobrazit všechny fotky“:** V `components/public/YearDetail.tsx` je galerie v modalu defaultně omezena na prvních 6 fotek. Pod galerií je přepínač „Zobrazit všechny fotky“ / „Zobrazit méně fotek“ se stejným vizuálním stylem jako CTA v archivu (hover fill do vínové, badge `+N` v acid green při sbaleném stavu).
- **Archiv – CTA „Zobrazit všechny ročníky“ vizuál + hover:** V `components/public/Archive.tsx` je tlačítko pro odhalení dalších ročníků stylované dle referenčního vzhledu (vínový border, výrazný hover fill do vínové, bílý text na hoveru, jemný stín). Při sbaleném stavu navíc zobrazuje badge `+N` (počet skrytých ročníků) v acid green.
- **Archiv – výchozí zobrazení jen 6 ročníků:** V `components/public/Archive.tsx` se bez aktivního vyhledávání defaultně renderuje pouze prvních 6 ročníků (grid i timeline). Pod seznamem je přepínací tlačítko „Zobrazit všechny ročníky“ / „Zobrazit méně ročníků“. Při vyhledávání se omezení vypíná a zobrazí se všechny nalezené výsledky.
- **Dev: 404 na /_next/static (rozbité styly):** Příčina často `EMFILE: too many open files` kvůli sledování obří složky `data/` a dalších. V `next.config.mjs` je `webpack.watchOptions.ignored` pro `data/`, `figma-404/`, `figma-seed/`, zipy (+ node_modules, .git). Postup obnovení: zastavit dev server, `rm -rf .next`, `npm run dev`, tvrdý refresh; případně `ulimit -n 10240`. Návod doplněn do `KROKY.md`.

- **Archiv – modal (YearDetail):** Experiment s `createPortal` + odloženým zavíráním byl zrušen (mohl rozbít chování/hydrataci); modal je znovu v DOMu uvnitř archivu jako dřív, uvnitř bílého panelu zůstává `stopPropagation` pro klik i `pointerDown` (menší riziko divného chování na dotyku). `Navbar` mobilní menu: při zavření `pointer-events: none` (kliky procházejí).
- **Archiv – fix rozkliknutí při hledání:** Ve filtru archivu byl bug `winner?.name.toLowerCase()` (při chybějícím vítězi bílých/červených vín to házelo `undefined.toLowerCase` a shodilo celý client). Opraveno přes `(name ?? "").toLowerCase()` a `search.trim()` před filtrem.
- **Archiv – Prehistorie 1988–2001 jako jedna speciální karta:** V `components/public/Archive.tsx` se roky 1988–2001 už nezobrazují jako samostatné karty; místo nich je vložená jedna karta „Prehistorie“ s odlišným obsahem v grid/timeline. Přidán centrální soubor `lib/prehistory.ts` s pevnými daty soupisu. `components/public/YearDetail.tsx` má pro tuto kartu vlastní layout modalu (jen seznam prehistorických ročníků), bez standardních sekcí Téma/Vítězové/Výsledky/Fotogalerie. Pro konzistenci je doplněna i stránka `app/archiv/[id]/page.tsx` s podporou `id=prehistorie`.
- **Vercel build (TS):** U výsledků vín po fallback dotazu bez sloupce `sample_number` doplněna normalizace řádků (`normalizeArchiveWineRows` v `/archiv/[id]`, `normalizeWineResultRows` v `lib/fetch-years.ts`), aby `next build` prošel typecheckem.
- **Archiv – Královna sklepa bez vína:** V `Archive.tsx` (karty grid i timeline) se u vítěze s prázdným `wine` za pomlčkou zobrazuje text „Název vzorku nedochován“; vyhledávání umí najít ročník i podle tohoto fallbacku.
- **Data fix prohozených sloupců ve výsledcích:** Přidán jednorázový skript `scripts/fix-swapped-sample-number.ts`, který v tabulkách `year_results_white` a `year_results_red` detekuje řádky, kde je zjevně prohozené `sample_number` a `wine` (název vína ve `sample_number`, číslo ve `wine`) a provede swap hodnot. Skript byl spuštěn na DB a opravil celkem 37 řádků (rok 2017: bílá 8, červená 29).
- **Data fix prohozených sloupců – rozšířená detekce:** Skript `scripts/fix-swapped-sample-number.ts` byl rozšířen o rozpoznání formátu vzorku `č.1` / `c.1` (nejen čistá čísla) a znovu spuštěn napříč všemi ročníky. Opraveno celkem 334 řádků (bílá: 76, červená: 258) v ročnících 2016, 2019, 2020, 2022, 2023, 2024, 2025, 2026.
- **Hotfix kompatibility výsledků bez migrace:** `fetchYearsWithData()` a `/archiv/[id]` mají fallback dotaz pro `year_results_white/red` – nejdřív zkusí načíst `sample_number`, a pokud sloupec v DB ještě neexistuje, automaticky přejdou na starý select bez `sample_number`. Díky tomu se výsledky znovu zobrazují i před spuštěním nové migrace.
- **Výsledky vín: přidán sloupec „Vzorek č.“ napříč BE/FE:** Přidána migrace `20260409000100_add_sample_number_to_wine_results.sql` (sloupec `sample_number` v `year_results_white` a `year_results_red`). `YearResultsForm` v adminu nyní umí číslo vzorku načítat, editovat a ukládat (včetně paste režimu „jméno; vzorek č.; víno; body“). Ve veřejných komponentách (`CurrentEdition`, modal `YearDetail`, stránka `/archiv/[id]`) jsou výsledky vín rozšířené na pořadí, výrobce, vzorek č., víno, body; archivní detail má nově záhlaví tabulky stejně jako highlight aktuálního ročníku.
- **Fonty 1:1 dle ČŮČO.zip:** Všechny komponenty veřejného webu používají `var(--font-bebas)` a `var(--font-inter)` místo literálů ‚Bebas Neue‘ / ‚Inter‘, aby se vždy aplikovaly fonty načtené přes next/font. V layoutu Inter s váhami 400, 500, 600, 700 (jako v zipu). Pushnuto (commit 0302ff1).
- **Úklid projektu:** Odstraněna složka `figma-export/` (reference je ČŮČO.zip). Odstraněny prázdné složky `data/`, `types/`. Z tsconfig vypuštěn exclude figma-export. Dokumentace (AGENTS.md, ZADANI_DOKONCENI.md, docs/working-memory.md, docs/decisions.md) upravena – design reference = ČŮČO.zip. V Navbar použitá konstanta ACID_GREEN místo literálu.
- **Frontend 1:1 dle ČŮČO.zip:** Veřejná homepage přepsána podle referenčního zipu. Hero – jen badge nadcházejícího ročníku a dva CTA (Zobrazit poslední ročník, Procházet archiv). Navbar – O akci, Aktuální ročník, Archiv, Místo konání, Kontakt. About – bez testimoniálů, styly dle zipu. CurrentEdition – tmavá sekce, vítězové + téma + accordion výsledků + fotogalerie s lightboxem; data z `fetchYearsWithData()`. Archive – grid/timeline, vyhledávání, po kliknutí na ročník modal YearDetail (ne odkaz na `/archiv/[id]`). Location, Contact, Footer – obsah a styly 1:1 ze zipu. Přidány `lib/year-data.ts`, `lib/fetch-years.ts`; odstraněn `ArchiveClient.tsx`. Stránka předává plná data ročníků (včetně výsledků a galerie) z DB.
- **Fotogalerie:** Migrace `20250313000000_storage_year_gallery.sql` – bucket `year-gallery`. Admin: YearGalleryForm. Veřejný detail v modalu YearDetail.
- **Vercel build:** createClient() bez throw při chybějících env (fallback ""), sitemap.ts podmíněné volání DB; build projde i bez env, pro běh je nutné nastavit NEXT_PUBLIC_SUPABASE_* na Vercelu.
- **Admin + veřejný detail ročníku:** YearForm + program, YearResultsForm, `/archiv/[id]` program a výsledky.
- **Druhá migrace nasazena:** db push, idempotentní INSERT upcoming_event.
- Založena struktura pro persistentní agenta: AGENTS.md, .cursor/rules, docs/working-memory.md, docs/decisions.md.
 - **Omezení adminu na e-maily (volitelné):** V `app/admin/(protected)/layout.tsx` se při přístupu do `/admin` kontroluje whitelist e-mailů z env proměnné `ADMIN_ALLOWED_EMAILS` (čárkou oddělené). Pokud je proměnná prázdná/není nastavena, mají přístup všichni přihlášení uživatelé; pokud je nastavená, do adminu se dostanou jen uživatelé s e-mailem v seznamu. Příklad je v `.env.example`.
 - **Paste výsledků z Excelu/CSV (admin):** V `YearResultsForm` jsou u sekcí „Bílá vína“ a „Červená vína“ textarea v collapsible blocích, kam lze vložit řádky z Excelu/CSV (oddělovač `;` nebo tab), ve formátu „jméno; víno; body“. Tlačítky lze buď přepsat celý seznam bílých/červených vín, nebo vložit nové řádky na konec.
 - **Fix nadcházejícího ročníku:** Stránka `/admin/upcoming` načítá hodnoty z `upcoming_event` a `UpcomingForm` při uložení nejdřív smaže všechny existující řádky a poté provede `upsert` jednoho řádku s fixním `id` (`00000000-0000-0000-0000-000000000001`). Tím garantuje, že je v tabulce vždy maximálně jeden řádek a nepadá chyba „duplicate key value violates unique constraint 'upcoming_event_one_row'“ ani pokud v tabulce historicky vznikly duplicity.
 - **Testovací pravidlo pro nové funkce:** V `.cursor/rules/testing-new-features.mdc` je pravidlo popisující, jak testovat novou funkcionalitu (identifikace flow, CRUD scénáře, zachytávání chyb, lint, aktualizace working-memory/decisions).
 - **Demo ročník 2025:** Přidána migrace `20250314000000_seed_demo_year_2025.sql`, která idempotentně vytvoří/aktualizuje ročník 2025 (XXXVIII) v tabulce `years` včetně programu (nadpis, autor, popis, dummy obrázek a PDF) a všech výsledků (Královna sklepa, Cena diváků, Sračka roku, top 5 bílých a červených vín). Migrace je nasazená na vzdálené Supabase.
 - **Seed galerie 2025 z reálných screenshotů:** Migrace `20250314010000_seed_year_2025_gallery.sql` plní tabulku `year_gallery` pro rok 2025 třemi položkami (`2025/about-1.png` až `about-3.png`). Soubory jsou nahrané v bucketu `year-gallery` pomocí Supabase CLI ze screenshotů (assets v Cursoru), takže aktuální ročník má kompletní galerii bez ručního uploadu.
 - **Hromadný import fotek v adminu:** `YearGalleryForm` umí kromě klasického uploadu i import z URL – collapsible blok „Hromadný import z URL“. Do textarey lze vložit seznam URL (každý řádek jedna fotka, např. z původního webu); po kliknutí na tlačítko se URL uloží do `year_gallery` s navazujícím `sort_order`. Veřejný web umí nově zobrazit jak obrázky ze Storage (`year-gallery/{path}`), tak přímo absolutní URL uložené v `storage_path`.
- **Admin users + bucket pro program:** Přidána migrace `20250314020000_admin_users.sql` s tabulkou `admin_users` (seznam e-mailů s přístupem do adminu). Layout `app/admin/(protected)/layout.tsx` nejdřív kontroluje `ADMIN_ALLOWED_EMAILS` (pokud je nastavené), jinak se pokusí ověřit uživatele proti `admin_users`; pokud tabulka neexistuje, chová se jako dřív (přístup pro všechny přihlášené). Přidána migrace `20250314021000_storage_year_program.sql` s bucketem `year-program` (obrázek/PDF programu).
- **Upload programu v adminu:** `YearForm` nově umožňuje nahrát obrázek a PDF programu přímo do bucketu `year-program` (získá se public URL), ale stále podporuje i vložení externí URL; veřejný detail ročníku používá stejné sloupce `program_image_url` / `program_pdf_url`, takže funguje beze změn.
- **Duplikace ročníku + filtrování v přehledu:** Na stránce `/admin/years` je v `YearsTable` přidaný filtrační panel (stav: vše/draft/publikováno, textové hledání podle roku/názvu/pořadí). V tabulce se u každého řádku objevila akce „Duplikovat“, která v client komponentě načte záznam z `years`, založí nový ročník ve stavu `draft` (název doplní `"(kopie)"`) a převezme program (titulek, autor, popis, obrázek/PDF); výsledky ani galerii zatím nekopíruje. Při duplikaci se novému záznamu vyplňuje i sloupec `name` (title/edition/year), aby nepadal NOT NULL constraint.
- **Zákaz dvou publikovaných ročníků se stejným rokem/názvem:** Přidána migrace `20250314022000_years_unique_published_year_title.sql`, která zavádí unikátní indexy na `year` a `title` pro řádky se statusem `publikováno`. V `YearForm` se před uložením ročníku ve stavu `publikováno` kontroluje, jestli už existuje jiný publikovaný ročník se stejným rokem nebo názvem; v takovém případě se zobrazí chybová hláška a ročník se neuloží.
- **Fallback hlášky pro archiv:** Ve `CurrentEdition` přibyl fallback text pro situaci, kdy má aktuální ročník prázdné výsledkové tabulky (zobrazí se chytré vysvětlení místo prázdného akordeonu) a kdy má označenou galerii, ale zatím bez fotek (tmavý box s vtipným textem o „vyvolávání analogů“). V modalu `YearDetail` se při chybějícím tématu/programu nově zobrazí informativní karta o tom, že program zatím není přepsaný z kronik. Stránka `/archiv/[id]` má jemně vtipnější a konkrétnější hlášku pro případ, kdy pro daný ročník nejsou v DB zatím žádné výsledky, program ani galerie.
- **Import archivu z data/**: Skript `npm run import:years` byl spuštěn bez `--dry-run` a prošel všechny ZIPy 2002–2026. Pro většinu ročníků byly do Supabase Storage nahrány fotky (bucket `year-gallery`) a programová PDF (bucket `year-program`) a do DB doplněny záznamy v `years`/`year_gallery`. U několika starších ročníků (např. 2004–2007) RLS politika blokovala zápis (chyba „new row violates row-level security policy“), takže jejich fotky/programy skript přeskočil – jsou vidět v `docs/archive-import-report.md` a případně se dají doplnit ručně nebo po úpravě RLS/migrace.
- **Prehistorie 1988–2001:** Přidán skript `scripts/import-prehistory.ts` a npm script `npm run import:prehistory`. Skript založí/aktualizuje v tabulce `years` záznamy pro roky 1988–2001 (edice I.–XIV., název „{edition}. ročník – Čůčobraní {year}“, status publikováno, `program_pdf_url` odkazující na `year-program/prehistorie-1988-2001.pdf`). Import byl spuštěn a všech 14 ročníků je v DB. PDF prehistorie je potřeba ručně nahrát do bucketu `year-program` pod cestu `prehistorie-1988-2001.pdf`, aby odkaz fungoval.
- **Retry import po opravě service key / RLS:** Po zprovoznění načítání `SUPABASE_SERVICE_ROLE_KEY` byl import znovu spuštěn pro roky 2004–2026 (`--years=...`) a úspěšně nahrál fotky i programová PDF, které dříve padaly na „new row violates row-level security policy“. Importní skript byl upraven tak, aby před vložením nové galerie nejdřív smazal existující řádky v `year_gallery` pro daný ročník (bez duplikací při opakovaných bězích). Zůstávají jen jednotlivé vadné JPG, které `sharp` nedokázal zpracovat (např. pár souborů v roce 2020).

## Constraints

- Design a funkcionalita veřejného webu 1:1 podle ČŮČO.zip (referenční obsah); živý kód v components/public/ a lib/theme.ts.
- Admin jen pro organizátory; jednoduchý, desktop-first.

## Open issues

- První admin uživatel se zakládá v Supabase (Authentication → Users → Add user).
- Fotogalerie a bucket `year-gallery` jsou hotové a migrace nasazené; ročník 2025 má demo galerii i data výsledků, další ročníky je možné naplnit přes admin (upload nebo hromadný import URL).
- Archiv 1988–2026 je v DB naplněný (prehistorie 1988–2001 přes `npm run import:prehistory`, 2002–2026 přes `npm run import:years`). PDF prehistorie je potřeba ručně nahrát do Storage `year-program/prehistorie-1988-2001.pdf`.

## Next recommended step

- Implementovat jednorázový skript `scripts/import-years-from-data.ts`, který:
  - projde všechny ZIPy v `data/`, pro každý rok najde kořenovou složku `{year}/`,
  - najde všechny obrázky pod `{year}/`, zkomprimuje je (sharp, delší strana max ~1600 px, quality ~80) a nahraje do bucketu `year-gallery` pod názvy `year-gallery/{year}/CUCOBRANI-{year}_{img-number}.jpg`, současně zapíše záznamy do tabulky `year_gallery` se správným `year_id` a `sort_order`,
  - najde programová PDF (preferenčně `Program {year}.pdf` / „Program“ v názvu), nahraje hlavní program do bucketu `year-program` jako `{year}-program.pdf` a uloží URL do polí `program_pdf_url` v tabulce `years`,
  - u každého ročníku dopočítá pořadí ročníku od roku 1988 (první ročník = 1988) a složí konzistentní název ročníku ve stylu `\"{edition}. ročník – Čůčobraní {year}\"`, uložený v `title`/`name`,
  - sesbírá soubory s výsledky (xls/xlsx/pdf) a podle potřeby připraví podklady pro částečný automatický import výsledků nebo ruční doplnění přes admin.
- Do skriptu přidat **dry-run mód** (`--dry-run`) a volitelný výběr ročníků (`--years=2010,2011`), aby bylo možné import bezpečně otestovat.
- Generovat po běhu skriptu přehledný report `docs/archive-import-report.md`, kde pro každý rok bude shrnutí (kolik fotek, program ano/ne, jaké soubory s výsledky) a seznam „extra“ souborů (plakáty, pozvánky, hudba, reportáže), které zatím neumíme automaticky zařadit, ale lze je později využít v UI.
- Navržené fallback hlášky pro chybějící data (výsledky, program, galerie, highlight, archiv listing) promítnout do komponent `YearDetail`, `CurrentEdition` a `/archiv/[id]` tak, aby se při nekompletních datech zobrazovaly chytré univerzální texty místo prázdných sekcí.
- Pro „Prehistorii 1988–2001“: v import skriptu nahrát PDF do bucketu `year-program` (`prehistorie-1988-2001.pdf`), založit/doplnit ročníky 1988–2001 v tabulce `years` a připravit jednoduchý formát (CSV/JSON) pro ruční doplnění základních údajů (Královna sklepa, téma, krátký popis), který pak může další malý skript nasypat do DB.

## Notes for next session

- Klíčové zdroje pravdy: ZADANI_DOKONCENI.md (co je hotové / co zbývá), KROKY.md (nastavení), tento working-memory a decisions.md.
- Import archivu: adresář `data/` obsahuje ZIPy po ročnících (2002–2026) a jedno PDF „Prehistorie 1988–2001“. Dohodnutá pravidla:
  - Fotky pro galerii = všechny obrázky (`*.jpg/jpeg/png`) kdekoliv pod složkou `{year}/` v ZIPu; při importu se zkomprimují a uloží jako `year-gallery/{year}/CUCOBRANI-{year}_{img-number}.jpg` (číslování od 1 podle seřazení).
  - Program = preferenčně soubory `Program {year}.pdf` (nebo jiné PDF s „Program“ v názvu); ukládají se do bucketu `year-program` jako `{year}-program.pdf` a URL se zapisuje do `years.program_pdf_url`.
  - Názvy ročníků = první ročník 1988 (1.), edice se počítá `editionNumber = year - 1988 + 1` a ukládá jako římská číslice; `title` je ve tvaru `\"{edition}. ročník – Čůčobraní {year}\"`.
  - Prehistorie: jedno PDF se nahraje jako `year-program/prehistorie-1988-2001.pdf`; obsah (Královna, téma) se doplní ručně pomocí pomocného CSV/JSON a speciálního importu.
- Na FE je navržen kompletní seznam fallback hlášek (vtipně laděných) pro případy, kdy chybí výsledky, program, galerie nebo jen část dat – ty je potřeba implementovat v `YearDetail`, `CurrentEdition` a `/archiv/[id]` spolu s připravovaným importem.
- Importní skript `scripts/import-years-from-data.ts` je implementovaný a připravený (komprese přes sharp, upload do Supabase Storage, zápisy do `years` a `year_gallery`, generování `docs/archive-import-report.md`, podpora `--dry-run` a `--years=`). Načítá env proměnné i z `.env.local` (přes `dotenv`), takže běží se stejnou konfigurací jako aplikace. Při chybě `unzip` typu „Illegal byte sequence“ daný rok nepoloží běh skriptu, ale označí ho v reportu jako problémový s jasnou poznámkou; pro plnohodnotný import těchto let je potřeba později buď opravit ZIPy, nebo připravit speciální import.
- FE fallbacky pro archiv jsou částečně implementované (aktuální ročník, modal detailu, stránka `/archiv/[id]`); případné další nuance (např. rozlišení mezi „žádné výsledky“ a „jen část výsledků“) lze dopracovat až podle reálných dat z importu.
- Po ostrém běhu import skriptu je archiv v DB/Storage reálně naplněný roky 2002–2026; některé starší roky mohou mít prázdnou galerii/program jen kvůli RLS chybě při importu (viz report). FE fallbacky tyto stavy už pokrývají, takže se UI chová rozumně i tam, kde data chybí.
